export function getMe(state) {
  return state.user;
}

// Getters of Adminator
export function getDrawerState(state) {
  return state.drawer;
}
export function getIsCollapsed(state) {
  return state.isCollapsed;
}
